import Layout from "~/components/Layout";
import React from "react";

/**
 * TODO list
 * - search bar?
 * - calendar view / search by date
 * - results
 */

const WritingHome = () => {
  return <Layout title="Thoughts, Musings, Blogging">todo</Layout>;
};

export default WritingHome;
